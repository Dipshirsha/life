<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Student Details List</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato">
  <style>
    /* Reset some default styles */
    body {
      font-family: 'Lato', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #252526, #010814);
      background-attachment: fixed;
      color: #fff;
    }

    /* Style the header */
    h1 {
      text-align: center;
      background-color: rgba(0, 0, 0, 0.5);
      color: #fff;
      padding: 20px;
      margin: 0;
      font-size: 2.5rem;
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    h2 {
      text-align: center;
      color: #ff6f61;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin-top: 30px;
      font-size: 2rem;
    }

    /* Style the student list */
    .student-list {
      list-style: none;
      padding: 0;
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      margin: 20px;
    }

    /* Style each student item */
    .student {
      width: 250px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      margin: 10px;
      padding: 20px;
      text-align: center;
      background-color: rgba(255, 255, 255, 0.1);
      border-radius: 10px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      backdrop-filter: blur(10px);
    }

    .student:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
    }

    /* Style student images */
    .student img {
      max-width: 50%;
      border-radius: 50%;
      border: 3px solid #ff6f61;
      transition: transform 0.3s ease;
    }

    .student:hover img {
      transform: scale(1.1);
    }

    /* Style student names */
    .student h2 {
      margin: 15px 0;
      font-size: 1.5rem;
      color: #fff;
    }

    /* Style additional student details */
    .student p {
      margin: 5px 0;
      color: #ddd;
      font-size: 1rem;
    }

    /* Social media icons */
    .social-icons {
      margin-top: 15px;
    }

    .social-icons a {
      color: #fff;
      margin: 0 10px;
      font-size: 1.5rem;
      transition: color 0.3s ease, transform 0.3s ease;
    }

    .social-icons a:hover {
      color: #ff6f61;
      transform: translateY(-5px);
    }

    /* Animation for student cards */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .student {
      animation: fadeIn 0.5s ease forwards;
      opacity: 0;
    }

    .student:nth-child(1) {
      animation-delay: 0.1s;
    }

    .student:nth-child(2) {
      animation-delay: 0.2s;
    }

    .student:nth-child(3) {
      animation-delay: 0.3s;
    }

    .student:nth-child(4) {
      animation-delay: 0.4s;
    }

    .student:nth-child(5) {
      animation-delay: 0.5s;
    }

    .student:nth-child(6) {
      animation-delay: 0.6s;
    }

    .student:nth-child(7) {
      animation-delay: 0.7s;
    }

    .student:nth-child(8) {
      animation-delay: 0.8s;
    }

    .student:nth-child(9) {
      animation-delay: 0.9s;
    }

    .student:nth-child(10) {
      animation-delay: 1s;
    }
  </style>
</head>

<body>
  <h1>Members Details</h1>
  <h2>Lead Details</h2>
  <ul class="student-list">
    <% for (let i = 0; i < locals.countdata; i++) { %>
      <% if (locals.userchange[i].role === "HR") { %>
        <li class="student">
          <img src="data:image/<%= userchange[i].photo.contentType %>;base64,<%= userchange[i].photo.imformation %>" />
          <h2><%= locals.userchange[i].name %></h2>
          <p><%= locals.userchange[i].email %></p>
          <p><%= locals.userchange[i].about %></p>
          <div class="social-icons">
            <a href="<%= locals.userchange[i].facebook %>"><i class="fab fa-facebook"></i></a>
            <a href="<%= locals.userchange[i].instagram %>"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-snapchat"></i></a>
            <a href="#"><i class="fab fa-pinterest"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-linkedin"></i></a>
          </div>
        </li>
      <% } %>
    <% } %>
  </ul>

  <h2>Member Details</h2>
  <ul class="student-list">
    <% for (let i = 0; i < locals.countdata; i++) { %>
      <li class="student">
        <img src="data:image/<%= userchange[i].photo.contentType %>;base64,<%= userchange[i].photo.imformation %>" />
        <h2><%= locals.userchange[i].name %></h2>
        <p><%= locals.userchange[i].email %></p>
        <p><%= locals.userchange[i].about %></p>
        <div class="social-icons">
          <a href="<%= locals.userchange[i].facebook %>"><i class="fab fa-facebook"></i></a>
          <a href="<%= locals.userchange[i].instagram %>"><i class="fab fa-instagram"></i></a>
          <a href="#"><i class="fab fa-snapchat"></i></a>
          <a href="#"><i class="fab fa-pinterest"></i></a>
          <a href="#"><i class="fab fa-twitter"></i></a>
          <a href="#"><i class="fab fa-linkedin"></i></a>
        </div>
      </li>
    <% } %>
  </ul>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>